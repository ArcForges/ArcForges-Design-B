# WP03.01 foundation contract implementation evidence

Status: **WP03.01 complete**. This receipt closes the selected foundation schema, safe-value and profile-validation boundary under the [approved profile](wp03-01-foundation-contract-profile.md) and [P2-017 validation policy](../decisions/phase-2-specification-decisions.md#rule-p2-017). The [machine-readable receipt](wp03-01-implementation-evidence.json) records reviewed source, successful required checks and complete normal publication. Substep 03.02 is next and has not started; Plan advancement is a separate completion change. This is [WP03.01](../planning/work-packages/03-contract-foundation-and-licence-split.md#rule-wp-03.01) acceptance, not completion of WP03 or commercial product acceptance.

## Authority and accepted source

[WP03.00 completion](wp03-00-implementation-evidence.md) supplies Contracts source `30ddcad2bcb3634e089abb5e29d6c9ce05d38386`, its existing 22 package identities and the latest upstream NuGet/npm `1.0.0-ci.86.1` publication. The separate Maven development channel remains `1.0.0-SNAPSHOT`; downstream consumer pins are unchanged.

[Design PR 62](https://github.com/ArcForges/ArcForges-Design/pull/62), merged as `3d4a58d6dc613d04ef12ed6db68fb41a0b14501d`, established the selected closure, value boundaries and complete ordered implementation plan. [Design PR 63](https://github.com/ArcForges/ArcForges-Design/pull/63), merged as `5e202ff10f3c218d9e159029579ca535c641169b`, aligned the framing vocabulary before dependent implementation.

[Contracts PR 36](https://github.com/ArcForges/Contracts/pull/36) received complete source, generated-output, fixture, provenance and workflow review at final head `30f7d4b8a785e1a71b1a62d3127d95fc4009aae7`. All applicable checks passed in [PR CI 35729559580](https://github.com/ArcForges/Contracts/actions/runs/35729559580) and [Security 35729559585](https://github.com/ArcForges/Contracts/actions/runs/35729559585), including Build candidate and Verify. Publication jobs were correctly skipped for the pull-request event. The PR merged normally as `4b8134eaf8a4174922d6378da003ed390b85a94a` at `2026-09-22T12:55:57Z`; the Contracts primary was fast-forwarded cleanly to that merge. Branch `wp03-01-foundation-contracts` and its worktree remain retained.

## Implemented boundary and evidence

| Condition | Verified result |
|---|---|
| Complete selected schema closure | `eng/foundation-inventory.json` binds 148 records: 32 Foundation and 116 PublicApi, with 10 enums, 82 oneof branches and all 16 AggregateBody branches. Reachability, complete fields/tags/presence and single ownership determine scope; counts do not replace those checks. |
| Safe value boundaries | 69 identifier domains, exact int64/uint64 and decimal boundaries, distinct Cloud/native/local revision and sequence types, opaque cursors, checked rational conversion and compatible unknown-read preservation use generated wire messages and explicit adapters. |
| Profile validation | Content origin, Notes scalar/query and Scope measurement have independent valid/invalid contract examples. Error category/effect/retry rules, rich-text UTF-16 boundaries, measurement binding and fixed-binary framing constraints are checked without executing product engines. |
| Independent C#/TS conformance | Both languages passed 476 cases: 314 positive and 162 negative. All 314 positive cases passed exchange in both directions, covering every selected record and oneof branch, 45 error-category mappings and 10 authored binary oracles. Ten original-profile scenario groups retain their later-owner boundary. Negative nominal-type/revision compilation passed. |
| Generation and source boundaries | Deterministic regeneration, eight inventory negative tests, 14 access tests, compiled ownership/import checks and the Apache source boundary passed. The selected package set remains 14 NuGet, five npm and three Maven identities; no new package, service listener or downstream dependency upgrade is introduced. |
| Local compiler observations | Windows C# Release compilation passed with zero warnings/errors using existing SDK `10.0.401` through the recorded local adapter; committed and CI SDK remains `10.0.400`. TypeScript workspaces compiled. Cached JDK 17 Kotlin/Java `assemble`/`check` passed, but test tasks reported `NO-SOURCE`. These are not Kotlin runtime or complete three-language conformance results. |
| Final PR candidate and security | The configured Ubuntu candidate/verification and applicable security checks passed for the reviewed head. This establishes the PR candidate gates, not publication or Windows CI coverage. |

Independent review fixed mixed-unit threshold binding, cursor-result agreement, binary field/checksum overlap, complete date/link matching, Unicode fixture preservation, union coverage and exchange case uniqueness. The first CI run additionally exposed a polynomial URL regex; the authoritative C#/TS helper now uses linear scheme/body/whitespace checks. Focused tests passed 96 URL expectations in each language, including a 100,000-character authority.

The documentation oracle failure exposed inherited Dokka parser links pointing to unrelated message types. The protobuf documentation now suppresses inherited runtime members while retaining every declared API marker, including `Id.hasValue`. The r6 successor retains 14,017 API members and all 152 prior API-marker pages; 453 protobuf pages changed and none were removed. Its 481 inputs, 42 fixed resources, 10 excluded fonts and 81 third-party components remain bound. Six focused admission tests passed, and final PR candidate validation passed with strict page/resource identity and mandatory marker checks intact. Exact public-hash secret-scan exceptions remain restricted to their reviewed rule, path and complete line.

Immutable `contract-bindings-r4`, `dokka-documentation-resources-r6` and dependency receipt `wp03-01-r2` bind the repaired source snapshot `af7b6cca5e14a51735512d283b03d3ae9d96d170`. Their predecessors remain unchanged. All 140 admitted dependency coordinates and their integrity/licence closure are preserved.

## Accepted normal publication

The accepted merge's [main CI 35730204418](https://github.com/ArcForges/Contracts/actions/runs/35730204418), run 89 attempt 1, and [main Security 35730204268](https://github.com/ArcForges/Contracts/actions/runs/35730204268) both succeeded. The required [Build candidate](https://github.com/ArcForges/Contracts/actions/runs/35730204418/job/106753582190) and [Verify](https://github.com/ArcForges/Contracts/actions/runs/35730204418/job/106755504306) jobs succeeded for source `4b8134eaf8a4174922d6378da003ed390b85a94a`.

[Publish NuGet](https://github.com/ArcForges/Contracts/actions/runs/35730204418/job/106755544028) recorded all 14 actual package uploads at **`1.0.0-ci.89.1`**. [Publish npm](https://github.com/ArcForges/Contracts/actions/runs/35730204418/job/106755545511) uploaded all five packages at that same version through OIDC with signed provenance. [Publish Maven channel](https://github.com/ArcForges/Contracts/actions/runs/35730204418/job/106755544995) executed all three publication tasks and completed the actual 15-file Sonatype **`1.0.0-SNAPSHOT`** upload; this was not a skipped or superseded-candidate no-op. Each publisher verified the original `1.0.0-ci.89.1` candidate handoff. The separate SNAPSHOT channel is not a formal stable Maven Central release.

The original `contracts-candidate-35730204418-1` artifact and its `artifacts/packages/manifest.json` remain the candidate file/hash authority. Maven's retained provider receipt is `maven-deployment-35730204418-1`, artifact ID `10695207684`. The coordinator read each publication job log once and recorded its actual provider result; no public package was downloaded, extracted or rehashed. These were the normal configured publications from the accepted merge, with no verification-only tag, version, signing or republication.

## Review and remaining gates

The expected Contracts merge, required main build/publication results and clean primary fast-forward are confirmed. Post-merge verification was limited to those observations and the clean update; it did not initiate an additional build, artifact-download, installation or runtime-test cycle. Source branches and worktrees remain retained. This Design documentation contribution follows complete review and JSON/link/document consistency checks; the repository has no configured CI and needs no product build or runtime test. A separate retained Plan worktree updates Current task to 03.02 under the approved plan; that next substep still requires its own research, decisions and complete ordered plan.

WP03.02 retains serialization/AOT/registration; .03 retains complete descriptor/resource/Sync admission semantics; .04 retains helper/in-process closure; .05 retains complete operation, authorization/scope, stream/history and three-language conformance; .06/.07/.90 retain compatibility, signed-format and complete-stage acceptance. WP04 still owns application foundation values. Generating a dependency early does not close its later semantic or integration gate.

No Notes query engine, Scope numerical engine, owner transaction, native/media effect, export/import pipeline, live authorization/provider or real AI inference was exercised here. No macOS, physical-device/emulator, GUI/browser E2E, installed-package consumer, public-release installation/upgrade, formal stable Maven Central release or complete Native AOT result is claimed. WP03 as a whole and commercial product acceptance remain open.
