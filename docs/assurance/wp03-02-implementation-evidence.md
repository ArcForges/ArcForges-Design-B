# WP03.02 serialization posture implementation evidence

Status: **WP03.02 complete**. This receipt closes the selected serialization, decode-limit, explicit-registration and Native AOT posture under the [approved profile](wp03-02-serialization-posture-profile.md) and the [P2-017 validation policy](../decisions/phase-2-specification-decisions.md#rule-p2-017). The [machine-readable receipt](wp03-02-implementation-evidence.json) records reviewed source, successful required checks and complete normal publication. Substep 03.03 is next and has not started; Plan advancement is a separate completion change. This is [WP03.02](../planning/work-packages/03-contract-foundation-and-licence-split.md#rule-wp-03.02) acceptance, not completion of WP03, closure of [F-026](open-gates-register.md#rule-f-026) or commercial product acceptance.

## Authority and accepted source

[WP03.01 completion](wp03-01-implementation-evidence.md) supplies Contracts source `4b8134eaf8a4174922d6378da003ed390b85a94a`, published as NuGet/npm `1.0.0-ci.89.1` with the separate Maven `1.0.0-SNAPSHOT` channel. [Design PR 65](https://github.com/ArcForges/ArcForges-Design/pull/65), merged as `212825003ed712200e585445301473233404f597`, fixed the profile, the ordered plan and the registry §8 clarification of nesting depth and strict HTTP-exception JSON.

[Contracts PR 37](https://github.com/ArcForges/Contracts/pull/37) received complete source, generated-output, fixture, provenance and workflow review at final head `affcaf177243fd8d3ec06477403ff156ccacebbc`. Its first CI run failed in build-identity inspection because the probe, which selects its host RID in the project, builds under a RID directory; the inspection now resolves that path and a scoped fix passed locally before the push. All applicable checks then passed in [PR CI 35796744177](https://github.com/ArcForges/Contracts/actions/runs/35796744177) and [Security 35796744184](https://github.com/ArcForges/Contracts/actions/runs/35796744184). Publication jobs were correctly skipped for the pull-request event. The PR merged normally as `e6c4a77f3ba48d70de4bf524623985b29278c784` at `2026-09-22T23:24:36Z`; the Contracts primary was fast-forwarded cleanly to that merge. Branch `wp03-02-serialization-posture` and its worktree remain retained.

## Implemented boundary and evidence

| Condition | Verified result |
|---|---|
| Binary codec and limits | C# `ContractWire`/`WireLimits` in Foundation and TypeScript `decodeContract`/`encodeContract`/`wireLimits` in `@arcforges/proto` apply 4 MiB unary/helper, 256 KiB inline page, 32 KiB stream frame and 64 MiB large projection bounds, the root plus 100 nested levels, typed `tooLarge`/`tooDeep`/`malformed` refusals and unknown-field retention. C# uses span parsing, which checks the end-of-stream tag, and fails closed if the runtime nesting default differs from 100; protobuf-es uses `recursionLimit` 101. |
| Strict HTTP-exception JSON | The three generated `JsonSerializerContext` classes use strict source-generation options. Generated C#/TypeScript codecs enforce each schema's `x-arcforges-max-bytes`, UTF-8 without BOM, depth 32, duplicate-property refusal and integer lexemes before the closed schema, and validate serialized output. |
| Explicit registration | Generated C# `ContractServices.All` (PublicApi, Sdk.Contracts) and TypeScript `contractServices` list exactly the authored services; hosts bind the plugin's explicit `BindService` methods. No server reflection, assembly scanning or runtime registry is used. |
| Public transport | `createPublicGrpcWebTransport` fixes binary gRPC-Web with the shared read options and refuses codec overrides. |
| Forbidden serializers and discovery | `eng/check_serialization.py` rejects reflection serializer/discovery packages in all NuGet, npm and Gradle locks, runtime-selected well-known proto types, reflection/discovery APIs in production source, handwritten wire records outside generated owners, missing strict JSON options and stale catalogues. Nine offline tests passed, with negative cases for every rule. Every project disables reflection-based System.Text.Json. |
| Independent vectors | `fixtures/public/wp03-02.json` was committed before the implementation: 31 binary and 51 JSON cases plus expected catalogues and bound methods. It exposed one real divergence: Google.Protobuf's stream parser accepted a top-level unmatched end-group tag that protobuf-es refuses; the C# codec now refuses it. |
| Native AOT | The test-only probe roots all 13 C# libraries with trim/AOT diagnostics as errors. Linux x64 in PR and main CI, and Windows x64 locally, published with zero diagnostics and passed 31/31 binary and 51/51 JSON vectors, all 162 generated messages, both catalogues and both explicitly bound methods, with dynamic code unsupported. TypeScript passed the same vectors in seven tests. |
| Retained checks | Deterministic regeneration, the 476-case foundation suites and 314-case exchange, build identity, contract access (531 distribution files, 22 packages), licence boundary (31 projects), provenance, dependency admission and naming passed. |
| Receipts | Immutable `contract-bindings-r5` supersedes r4 for regenerated outputs. Dependency receipt `wp03-02-r1` admits only `Microsoft.DotNet.ILCompiler` and its `linux-x64`/`win-x64` runtime packages 10.0.11 as build-test tooling, with DesktopPlatform's reviewed nuspec evidence; 143 coordinates remain licence/integrity bound. The secret-scan allowlist binds the new receipt's exact public hash rows. |

Local C# validation used existing SDK `10.0.401` and existing MSVC tools through a recorded adapter selecting the committed 10.0.11 linker/compiler/runtime packs; committed and CI SDK remains `10.0.400`. No toolchain was installed.

## Accepted normal publication

The accepted merge's [main CI 35797272606](https://github.com/ArcForges/Contracts/actions/runs/35797272606), run 92 attempt 1, and [main Security 35797272589](https://github.com/ArcForges/Contracts/actions/runs/35797272589) both succeeded for source `e6c4a77f3ba48d70de4bf524623985b29278c784`. The required [Build candidate](https://github.com/ArcForges/Contracts/actions/runs/35797272606/job/106979366263) and [Verify](https://github.com/ArcForges/Contracts/actions/runs/35797272606/job/106980532075) jobs succeeded.

[Publish NuGet](https://github.com/ArcForges/Contracts/actions/runs/35797272606/job/106980562324) recorded all 14 package pushes at **`1.0.0-ci.92.1`**. [Publish npm](https://github.com/ArcForges/Contracts/actions/runs/35797272606/job/106980563011) published all five packages at that version through OIDC with signed provenance. [Publish Maven channel](https://github.com/ArcForges/Contracts/actions/runs/35797272606/job/106980562840) executed all three publication tasks to the Sonatype **`1.0.0-SNAPSHOT`** repository and retained provider receipt `maven-deployment-35797272606-1` (artifact ID `10723939938`). The original `contracts-candidate-35797272606-1` artifact remains the candidate file/hash authority. The coordinator read each publication log once; no public package was downloaded, extracted or rehashed, and no verification-only tag, version, signing or republication was created.

## Review and remaining gates

Post-merge verification was limited to the expected merge commit, the required main build/publication results and the clean primary fast-forward. Source branches and worktrees remain retained. This Design contribution follows complete review and JSON/link/document consistency checks; the repository has no configured CI.

F-026 receives this producer contribution — generated-only client pins, reflection-package absence and build-breaking AOT diagnostics over the contract closure — and stays open until WP06.02 proves a real generated-client call in the published host/client AOT artifacts. Transport frame enforcement stays with WP23/WP24/WP30. WP03.03 retains descriptor/resource/Sync admission semantics; .04 helper/in-process closure; .05 complete operations, native-auth/browser exceptions, Kotlin posture and three-language conformance; .06/.07/.90 compatibility, signed formats and stage acceptance.

No live RPC, browser/device, GUI, installed-package consumer, public-release installation/upgrade, macOS, formal stable Maven Central or commercial result is claimed. The Windows AOT result is a local observation; Linux AOT evidence comes from configured CI.
